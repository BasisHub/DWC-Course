---
phase: 03-navigation-design-polish
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/Hero/index.tsx
  - src/components/Hero/styles.module.css
  - src/components/ChapterCards/index.tsx
  - src/components/ChapterCards/styles.module.css
  - src/components/HomepageFeatures/index.tsx
  - src/components/HomepageFeatures/styles.module.css
  - docs/index.md
  - static/img/bbj-to-web.svg
  - static/img/css-layouts.svg
  - static/img/validation.svg
autonomous: true

must_haves:
  truths:
    - "Homepage displays a bold hero section with title, subtitle, and a prominent Start Learning CTA button"
    - "Homepage shows feature highlights section below the hero (text-focused, no SVG illustrations)"
    - "Homepage chapter cards are grouped into 4 sections matching the sidebar (Getting Started, Core Concepts, Advanced Topics, Deployment)"
    - "Chapter cards use clean typography without emoji icons"
    - "Cards have hover effects with blue accent border"
    - "CTA button links to the first chapter (/gui-to-bui-to-dwc)"
    - "A closing CTA section appears at the bottom of the homepage"
    - "All components render correctly in dark mode with appropriate contrast"
    - "Cards reflow to single column below 996px"
  artifacts:
    - path: "src/components/Hero/index.tsx"
      provides: "Hero section with title, subtitle, and CTA button"
      exports: ["default"]
    - path: "src/components/Hero/styles.module.css"
      provides: "Hero styling with responsive breakpoint"
    - path: "src/components/ChapterCards/index.tsx"
      provides: "Chapter cards grouped by 4 sections with section headings"
      exports: ["default"]
    - path: "src/components/ChapterCards/styles.module.css"
      provides: "Card styling with dark mode and hover effects"
      contains: "[data-theme='dark']"
    - path: "src/components/HomepageFeatures/index.tsx"
      provides: "Text-focused feature highlights (no SVGs)"
      exports: ["default"]
    - path: "src/components/HomepageFeatures/styles.module.css"
      provides: "Feature highlights styling"
    - path: "docs/index.md"
      provides: "Homepage MDX importing Hero, HomepageFeatures, ChapterCards"
      contains: "import Hero"
  key_links:
    - from: "docs/index.md"
      to: "src/components/Hero/index.tsx"
      via: "MDX import"
      pattern: "import Hero from '@site/src/components/Hero'"
    - from: "docs/index.md"
      to: "src/components/HomepageFeatures/index.tsx"
      via: "MDX import"
      pattern: "import HomepageFeatures from '@site/src/components/HomepageFeatures'"
    - from: "docs/index.md"
      to: "src/components/ChapterCards/index.tsx"
      via: "MDX import"
      pattern: "import ChapterCards from '@site/src/components/ChapterCards'"
    - from: "src/components/Hero/index.tsx"
      to: "/gui-to-bui-to-dwc"
      via: "Docusaurus Link component"
      pattern: "to=\"/gui-to-bui-to-dwc\""
    - from: "src/components/ChapterCards/styles.module.css"
      to: "src/css/custom.css"
      via: "CSS variable inheritance"
      pattern: "var(--ifm-color-primary)"
---

<objective>
Redesign the homepage with a modern, polished layout: hero section, text-focused feature highlights, and grouped chapter cards -- matching the clean/minimal visual tone described in CONTEXT.md.

Purpose: This is the core of DES-01 (homepage redesign) and DES-02 (consistent visual language). The homepage is the first thing prospects and clients see.
Output: Three React components (Hero, ChapterCards, HomepageFeatures) and a redesigned docs/index.md that composes them.
</objective>

<execution_context>
@/Users/beff/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beff/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-navigation-design-polish/03-CONTEXT.md
@.planning/phases/03-navigation-design-polish/03-RESEARCH.md
@.planning/phases/03-navigation-design-polish/03-01-SUMMARY.md
@src/components/ChapterCards/index.tsx
@src/components/ChapterCards/styles.module.css
@src/components/HomepageFeatures/index.tsx
@src/components/HomepageFeatures/styles.module.css
@docs/index.md
@docusaurus.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero component and redesign HomepageFeatures</name>
  <files>
    src/components/Hero/index.tsx
    src/components/Hero/styles.module.css
    src/components/HomepageFeatures/index.tsx
    src/components/HomepageFeatures/styles.module.css
    static/img/bbj-to-web.svg
    static/img/css-layouts.svg
    static/img/validation.svg
  </files>
  <action>
**Hero component (NEW -- create src/components/Hero/ directory):**

Create `src/components/Hero/index.tsx`:
- Import React types, clsx, Link from '@docusaurus/Link', Heading from '@theme/Heading'
- Render a `<header>` with centered layout:
  - `<Heading as="h1">` with the site title: "BBj DWC Training"
  - `<p>` subtitle: something like "Master the Dynamic Web Client with this comprehensive 12-chapter course" (use Claude's discretion for exact wording)
  - A `<Link>` button using Infima classes: `button button--primary button--lg` linking to `/gui-to-bui-to-dwc` with text "Start Learning"
- No background patterns or gradients. Clean whitespace. Centered text.

Create `src/components/Hero/styles.module.css`:
- `.hero`: padding 4rem 0, text-align center
- `.heroTitle`: font-size 3rem, font-weight 800 (or 700)
- `.heroSubtitle`: font-size 1.25rem, color var(--ifm-color-content-secondary), max-width 600px, margin auto, margin-bottom 2rem
- `.heroButtons`: flex, justify-content center, gap 1rem
- Responsive @media (max-width: 996px): reduce heroTitle to ~2rem, heroSubtitle to ~1rem

**HomepageFeatures redesign (MODIFY existing):**

Rewrite `src/components/HomepageFeatures/index.tsx`:
- Remove SVG imports entirely. Switch to text-focused feature highlights.
- Change the FeatureItem type: remove `Svg` field, keep `title` and `description` only.
- Use 3 feature highlights (Claude's discretion on content -- suggest something like):
  1. "12 Progressive Chapters" -- structured curriculum from basics to deployment
  2. "Hands-On Code Samples" -- every chapter includes runnable BBj source files
  3. "Modern Web Standards" -- learn CSS Grid, Flexbox, web components, and responsive design
- Each feature renders as a col col--4 card-like block with a heading and description. No SVGs, no icons.
- Use Infima grid classes (row, col col--4).

Rewrite `src/components/HomepageFeatures/styles.module.css`:
- Remove `.featureSvg` class.
- `.features`: padding 2rem 0, width 100%
- Add card-like styling for each feature if appropriate (border, padding, border-radius) matching the clean minimal tone. Use var(--ifm-color-emphasis-200) for borders.
- Add [data-theme='dark'] variants for any custom colors.

**Delete unused SVGs:**
Delete `static/img/bbj-to-web.svg`, `static/img/css-layouts.svg`, `static/img/validation.svg` -- no longer referenced after HomepageFeatures redesign.
  </action>
  <verify>Run `npm run typecheck`. No TypeScript errors in Hero or HomepageFeatures components. Confirm SVG files deleted.</verify>
  <done>Hero component exists with title, subtitle, and CTA button. HomepageFeatures uses text-only highlights without SVGs. All unused SVGs deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign ChapterCards with section grouping</name>
  <files>
    src/components/ChapterCards/index.tsx
    src/components/ChapterCards/styles.module.css
  </files>
  <action>
**ChapterCards redesign (MODIFY existing):**

Rewrite `src/components/ChapterCards/index.tsx`:
- Define a `SectionItem` type: `{ label: string; chapters: ChapterItem[] }`
- Change `ChapterItem` type: remove `number` and `icon` fields. Keep `title`, `slug`, `description`.
- Create a `sections` array with 4 sections matching the sidebar grouping:
  - "Getting Started": chapters 1 (GUI to BUI to DWC), 2 (Browser Developer Tools, CSS, and Themes), 3 (DWC Debugging)
  - "Core Concepts": chapters 4 (Upgrading Apps to DWC), 5 (DWC Controls With Extended Attributes), 6 (Flow Layouts and CSS for Responsive Design)
  - "Advanced Topics": chapters 7 (Icon Pools), 8 (Control Validation), 9 (Browser Constraints), 10 (Embedding 3rd Party Components), 11 (Advanced Responsive Design)
  - "Deployment": chapter 12 (Deployment Options)
- Keep the exact same slugs and descriptions from the current ChapterList.
- Render: For each section, show a section heading (`<Heading as="h2">`) with the label, then a `<div className="row">` containing ChapterCard components.
- Each ChapterCard: a `<Link to={slug}>` wrapping a card div. Show title as `<Heading as="h3">` and description as `<p>`. No emoji icon. No chapter number display.
- The entire card should be clickable (the Link wraps the card container).

Rewrite `src/components/ChapterCards/styles.module.css`:
- `.chapters`: padding 2rem 0
- `.section`: margin-bottom 3rem
- `.sectionTitle`: font-size 1.5rem, margin-bottom 1.5rem, font-weight 600
- `.card`: display block, padding 1.5rem, border 1px solid var(--ifm-color-emphasis-200), border-radius 8px, background var(--ifm-card-background-color), text-decoration none, color inherit, transition border-color 0.2s + box-shadow 0.2s, height 100%, margin-bottom 1rem
- `.card:hover`: border-color var(--ifm-color-primary), box-shadow 0 2px 12px var(--ifm-color-emphasis-100), text-decoration none, color inherit
- `.cardTitle`: font-size 1.1rem, margin-bottom 0.5rem
- `.cardDescription`: font-size 0.9rem, color var(--ifm-color-content-secondary), margin 0
- Dark mode: `[data-theme='dark'] .card`: border-color var(--ifm-color-emphasis-300)
- Dark mode hover: `[data-theme='dark'] .card:hover`: box-shadow 0 2px 12px rgba(0, 0, 0, 0.3)
- All col col--4 elements will auto-reflow to single column at 996px via Infima's built-in media query.
  </action>
  <verify>Run `npm run typecheck`. No TypeScript errors. Verify ChapterCards renders 4 sections with correct chapter counts (3, 3, 5, 1).</verify>
  <done>ChapterCards displays 12 chapters grouped into 4 labeled sections. Cards are clean, text-only, with hover effects and dark mode support.</done>
</task>

<task type="auto">
  <name>Task 3: Compose homepage MDX with all components and closing CTA</name>
  <files>docs/index.md</files>
  <action>
Rewrite `docs/index.md` as MDX with the redesigned layout:

Frontmatter:
```yaml
---
sidebar_position: 1
slug: /
title: BBj DWC Training
hide_table_of_contents: true
---
```

Body:
1. Import all three components:
   ```
   import Hero from '@site/src/components/Hero';
   import HomepageFeatures from '@site/src/components/HomepageFeatures';
   import ChapterCards from '@site/src/components/ChapterCards';
   ```
2. Render in order:
   - `<Hero />`
   - `<HomepageFeatures />`
   - `<ChapterCards />`
3. After ChapterCards, add a closing CTA section using plain MDX:
   - A centered section with a heading like "Ready to Get Started?" and a brief line, then a Link button to the first chapter.
   - Use Infima utility classes for centering: `<div className="text--center" style={{padding: '3rem 0'}}>` (inline style is acceptable for a one-off section, or use a simple CSS class).
   - The CTA link: `<a className="button button--primary button--lg" href="/gui-to-bui-to-dwc">Begin Chapter 1</a>` (or use the Link import from the Hero component -- but since this is MDX, a simple anchor with relative path works).

Remove all the old markdown content (Course Overview heading, Getting Started section, Sample Code section, Setup Requirements section). The new components handle all of that.

Add `hide_table_of_contents: true` to frontmatter to remove the right-side TOC on the homepage.
  </action>
  <verify>Run `npm run build`. Build succeeds. The homepage renders Hero, HomepageFeatures, ChapterCards, and a closing CTA. No broken links.</verify>
  <done>Homepage is a clean MDX page composing Hero + HomepageFeatures + ChapterCards + closing CTA. Old markdown content removed. Table of contents hidden.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with zero errors
2. `npm run typecheck` passes
3. Homepage shows hero, feature highlights, grouped chapter cards, and closing CTA
4. No emoji icons on chapter cards
5. Cards are grouped into 4 sections with headings
6. Dark mode: cards show appropriate borders and shadows (via [data-theme='dark'] CSS)
7. Responsive: at 996px, cards reflow to single column (via Infima col behavior)
8. No unused SVG files remain
</verification>

<success_criteria>
- Hero component renders with title, subtitle, and Start Learning button
- HomepageFeatures shows 3 text-only feature highlights
- ChapterCards shows 12 chapters in 4 grouped sections
- Closing CTA links to chapter 1
- All components have dark mode CSS variants
- Site builds and typechecks cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-navigation-design-polish/03-02-SUMMARY.md`
</output>
